function soma(n1=0, n2=0) { //A funcao soma recebe dois valores(O 0 será o valor da variavel não declarada)
     return n1 + n2                      // casocontrario será indefinido
}

console.log(soma(3, 8)) //Manda esctrever na tela o resultado de n1 +n2